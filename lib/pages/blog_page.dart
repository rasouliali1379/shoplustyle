import 'package:flutter/material.dart';
import 'package:shoplustyle/widgets/custom_text.dart';

class BlogPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: CustomText(text:"وبلاگ به زودی..."),
      ),
    );
  }
}
